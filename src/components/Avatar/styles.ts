import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 48,
    height: 48,
    borderRadius: 8,
    alignItems: "center",
  },
  avatar: {
    width: 46,
    height: 46,
    borderRadius: 8,
  },
});
